<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yaouguoji.platform.mapper.cameraMapper">
    <resultMap id="BaseResultMap" type="com.yaouguoji.platform.entity.camera">
        <id column="camera_id" jdbcType="INTEGER" property="cameraId" />
        <result column="c_name" jdbcType="VARCHAR" property="cName" />
        <result column="area_id" jdbcType="INTEGER" property="areaId" />
    </resultMap>

    <sql id="Base_Column_List">
        camera_id,
        c_name,
        area_id
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from camera
        where camera_id = #{cameraId,jdbcType=INTEGER}
    </select>

    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        delete from camera
        where camera_id = #{cameraId,jdbcType=INTEGER}
    </delete>

    <insert id="insert" parameterType="com.yaouguoji.platform.entity.camera">
        insert into camera (camera_id, c_name, area_id
          )
        values (#{cameraId,jdbcType=INTEGER}, #{cName,jdbcType=VARCHAR}, #{areaId,jdbcType=INTEGER}
          )
    </insert>

    <insert id="insertSelective" parameterType="com.yaouguoji.platform.entity.camera">
        insert into camera
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="cameraId != null">
              camera_id,
            </if>
            <if test="cName != null">
              c_name,
            </if>
            <if test="areaId != null">
              area_id,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="cameraId != null">
              #{cameraId,jdbcType=INTEGER},
            </if>
            <if test="cName != null">
              #{cName,jdbcType=VARCHAR},
            </if>
            <if test="areaId != null">
              #{areaId,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <update id="updateByPrimaryKeySelective" parameterType="com.yaouguoji.platform.entity.camera">
        update camera
        <set>
            <if test="cName != null">
              c_name = #{cName,jdbcType=VARCHAR},
            </if>
            <if test="areaId != null">
              area_id = #{areaId,jdbcType=INTEGER},
            </if>
        </set>
        where camera_id = #{cameraId,jdbcType=INTEGER}
    </update>

    <update id="updateByPrimaryKey" parameterType="com.yaouguoji.platform.entity.camera">
        update camera
        set c_name = #{cName,jdbcType=VARCHAR},
          area_id = #{areaId,jdbcType=INTEGER}
        where camera_id = #{cameraId,jdbcType=INTEGER}
      </update>
</mapper>